import React from "react";
import styled from "styled-components";

const StyledCenter = styled.div``;

const PointerCenter = (props) => {
  const { light, ...rest } = props;
  return <styledPointerCenter />;
};

export default PointerCenter;
